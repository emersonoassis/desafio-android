plugins {
    id 'com.android.application'
    id 'kotlin-android'
}


apply from: "$rootProject.projectDir/android-common.gradle"


dependencies {
    appDependencies.each { add(it.configuration, it.dependency) }
}
